import {chromium} from 'playwright';import {spawn} from 'node:child_process';import {mkdir,writeFile} from 'node:fs/promises';import assert from 'node:assert/strict';
const out='character-qa';await mkdir(out,{recursive:true});const external=process.env.QA_BASE_URL,base=external||'http://127.0.0.1:3017';const server=external?null:spawn(process.execPath,['server.mjs'],{env:{...process.env,PORT:'3017'},stdio:'ignore'});let browser;
try{
 for(let i=0;i<80;i++){try{const r=await fetch(base+'/character-check.html');if(r.ok)break;}catch{}await new Promise(r=>setTimeout(r,200));}
 browser=await chromium.launch({headless:true,args:['--no-sandbox','--enable-unsafe-swiftshader','--use-angle=swiftshader','--disable-dev-shm-usage']});const page=await browser.newPage({viewport:{width:1040,height:850}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(base+'/character-check.html');await page.waitForFunction(()=>window.zederCharacterCheck,{timeout:60000});
 const report=await page.evaluate(async()=>{
  const T=await import('/vendor/three.module.js'),c=window.zederCharacterCheck;c.step(0);const b=c.inspect().bones,dist=(a,d)=>b[a].getWorldPosition(new T.Vector3()).distanceTo(b[d].getWorldPosition(new T.Vector3()));const expected=[dist('UpperArmR','LowerArmR'),dist('LowerArmR','WristR'),dist('UpperArmL','LowerArmL'),dist('LowerArmL','WristL')],checks=[];
  for(const mode of ['idle','walk','write','read','carry','board','write','walk','idle']){c.setMode(mode);let maxDeviation=0,maxBlendError=0;for(let i=0;i<90;i++){c.step(1/30);const lengths=[dist('UpperArmR','LowerArmR'),dist('LowerArmR','WristR'),dist('UpperArmL','LowerArmL'),dist('LowerArmL','WristL')];maxDeviation=Math.max(maxDeviation,...lengths.map((v,k)=>Math.abs(v/expected[k]-1)));maxBlendError=Math.max(maxBlendError,Math.abs(Object.values(c.debug().weights).reduce((a,b)=>a+b,0)-1));for(const bone of Object.values(b))if(!bone.quaternion.toArray().every(Number.isFinite)||!bone.position.toArray().every(Number.isFinite))throw Error('Nonfinite bone');}c.root.updateMatrixWorld(true);checks.push({mode,maxDeviation,maxBlendError,size:new T.Box3().setFromObject(c.model).getSize(new T.Vector3()).toArray(),rig:c.debug().model,skinMeshes:c.debug().skinnedMeshes});}
  c.setMode('idle');c.step(.05);const at=c.debug();c.step(0);if(JSON.stringify(at)!==JSON.stringify(c.debug()))throw Error('Paused pose changed');return checks;
 });
 for(const c of report){assert.ok(c.maxDeviation<.035,`${c.mode}: arm length changed ${c.maxDeviation}`);assert.ok(c.maxBlendError<1e-6);assert.ok(c.size.every(Number.isFinite));assert.ok(c.size[1]>.9&&c.size[1]<2.4);assert.ok(c.skinMeshes>0);}
 for(const mode of ['idle','write','carry','board']){await page.evaluate(m=>{zederCharacterCheck.setMode(m);for(let i=0;i<40;i++)zederCharacterCheck.step(.05);},mode);await page.screenshot({path:`${out}/${mode}.png`});}await page.setViewportSize({width:390,height:844});await page.screenshot({path:`${out}/mobile.png`});assert.deepEqual(errors,[]);await writeFile(`${out}/checks.json`,JSON.stringify({base,checks:report,errors},null,2));console.log('PASS: 9 skeletal transitions, fixed arm lengths, finite bones, blend normalization, pause, mobile.');
}finally{await browser?.close();server?.kill();}
