# No credentials are needed for the clearly labeled local UX demo.
# All three values below are required to enable server-side LLM requests.
OPENAI_API_KEY=
OPENAI_MODEL=
# A long, random private-preview access code, NOT an OpenAI API key.
ZEDER_PREVIEW_KEY=
PORT=3000
